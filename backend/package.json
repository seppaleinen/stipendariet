{
  "name": "@stipendariet/backend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "docker compose -f ../docker-compose.yml build backend",
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000"
  }
}
