{
  "name": "stipendariet",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "StipendieAssistenten monorepo — grant assistant application for Swedish foundations",
  "packageManager": "pnpm@11.4.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "dev:infra": "docker compose -f docker-compose.yml up -d postgres redis browserless",
    "dev:stop-infra": "docker compose -f docker-compose.yml stop postgres redis browserless",
    "dev:all": "pnpm run dev:infra && turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "test:all": "turbo run test test:e2e",
    "ci:build-all": "turbo run build",
    "docker:build-backend": "docker compose -f docker-compose.yml build backend",
    "docker:up": "docker compose -f docker-compose.yml up -d",
    "docker:down": "docker compose -f docker-compose.yml down"
  },
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "eslint": "^9.32.0",
    "eslint-plugin-react-hooks": "^5.2.0",
    "eslint-plugin-react-refresh": "^0.4.20",
    "globals": "^15.15.0",
    "turbo": "^2",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.38.0"
  }
}
